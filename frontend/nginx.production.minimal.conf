# Solar Hub Frontend - Minimal Test Configuration
# Use this to test if basic serving works

server {
    listen 8080;
    listen [::]:8080;
    server_name _;

    root /opt/solarhub/app/solar-hub/frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
